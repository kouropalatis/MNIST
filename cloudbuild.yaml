steps:
  # Build backend image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/backend:$COMMIT_SHA'
      - '-t'
      - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/backend:latest'
      - '-f'
      - 'dockerfiles/backend.dockerfile'
      - '.'

  # Build frontend image
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/frontend:$COMMIT_SHA'
      - '-t'
      - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/frontend:latest'
      - '-f'
      - 'dockerfiles/frontend.dockerfile'
      - '.'

images:
  - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/backend:$COMMIT_SHA'
  - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/backend:latest'
  - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/frontend:$COMMIT_SHA'
  - 'europe-north2-docker.pkg.dev/$PROJECT_ID/mlops-repos/frontend:latest'

options:
  logging: CLOUD_LOGGING_ONLY